///
/// JMediaState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "MediaState.hpp"

#include "JMediaTrack.hpp"
#include "MediaTrack.hpp"
#include <optional>
#include <string>

namespace margelo::nitro::readium {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "MediaState" and the the Kotlin data class "MediaState".
   */
  struct JMediaState final: public jni::JavaClass<JMediaState> {
  public:
    static constexpr auto kJavaDescriptor = "Lcom/margelo/nitro/reactnativereadium/MediaState;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct MediaState by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    MediaState toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldState = clazz->getField<jni::JString>("state");
      jni::local_ref<jni::JString> state = this->getFieldValue(fieldState);
      static const auto fieldResourceIndex = clazz->getField<double>("resourceIndex");
      double resourceIndex = this->getFieldValue(fieldResourceIndex);
      static const auto fieldPosition = clazz->getField<double>("position");
      double position = this->getFieldValue(fieldPosition);
      static const auto fieldDuration = clazz->getField<jni::JDouble>("duration");
      jni::local_ref<jni::JDouble> duration = this->getFieldValue(fieldDuration);
      static const auto fieldTotalDuration = clazz->getField<jni::JDouble>("totalDuration");
      jni::local_ref<jni::JDouble> totalDuration = this->getFieldValue(fieldTotalDuration);
      static const auto fieldPlaybackRate = clazz->getField<double>("playbackRate");
      double playbackRate = this->getFieldValue(fieldPlaybackRate);
      static const auto fieldTrack = clazz->getField<JMediaTrack>("track");
      jni::local_ref<JMediaTrack> track = this->getFieldValue(fieldTrack);
      return MediaState(
        state->toStdString(),
        resourceIndex,
        position,
        duration != nullptr ? std::make_optional(duration->value()) : std::nullopt,
        totalDuration != nullptr ? std::make_optional(totalDuration->value()) : std::nullopt,
        playbackRate,
        track != nullptr ? std::make_optional(track->toCpp()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JMediaState::javaobject> fromCpp(const MediaState& value) {
      using JSignature = JMediaState(jni::alias_ref<jni::JString>, double, double, jni::alias_ref<jni::JDouble>, jni::alias_ref<jni::JDouble>, double, jni::alias_ref<JMediaTrack>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        jni::make_jstring(value.state),
        value.resourceIndex,
        value.position,
        value.duration.has_value() ? jni::JDouble::valueOf(value.duration.value()) : nullptr,
        value.totalDuration.has_value() ? jni::JDouble::valueOf(value.totalDuration.value()) : nullptr,
        value.playbackRate,
        value.track.has_value() ? JMediaTrack::fromCpp(value.track.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::readium
